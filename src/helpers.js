const colors = require('colors') // eslint-disable-line

// Just some fancy logging messages
exports.logging = {
  ELASTIC: `${'['.grey}${'Elastic'.red}${']'.grey}`,
  SLACK: `${'['.grey}${'Slack'.cyan}${']'.grey}`
}
